VITE_AI_PROXY_URL=https://precipice-ai-proxy.precipice.workers.dev
# The public, read-only viewer and publishing controls use this separate Worker.
VITE_PUBLICATION_API_URL=https://precipice-publications.precipice.workers.dev
# Public Turnstile site key for the invite-only publishing sign-in flow. This is not a secret.
# Create a local widget for localhost rather than reusing the production site key.
VITE_TURNSTILE_SITE_KEY=
