{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-credentials",
  "description": "Only the local main window can manage the one Anthropic credential.",
  "windows": ["main"],
  "platforms": ["macOS"],
  "permissions": ["allow-read-api-key", "allow-save-api-key", "allow-remove-api-key"]
}
